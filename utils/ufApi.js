// export async function useGetUfs(options = {}) {
//     const { data, error }  = await useApi('uf', {method: 'GET', ...options})

//     if (error.value) {
//         console.error('Erro ao buscar UF:', error.value);
//         return null;
//       }
//       return data.value;
// }